type ComponentFilterType = {
    category: string,
    items: string[],
    position: number,

}


export default function ComponentFilterRow({ category, items, position }: ComponentFilterType) {

    return (
        <div>

        </div>
    )
}